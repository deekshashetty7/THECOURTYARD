  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the frontend and backend together.

  Run `npm run dev:client` for frontend only, or `npm run dev:server` for backend only.

  
